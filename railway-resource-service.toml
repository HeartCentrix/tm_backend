[build]
builder = "DOCKERFILE"
dockerfilePath = "services/resource-service/Dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --workers 2"
healthcheckPath = "/health"
healthcheckTimeout = 300

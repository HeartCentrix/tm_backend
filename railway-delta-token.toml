[build]
builder = "DOCKERFILE"
dockerfilePath = "services/delta-token/Dockerfile"

[deploy]
startCommand = "python -m uvicorn services.delta-token.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300

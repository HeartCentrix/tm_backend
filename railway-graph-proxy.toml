[build]
builder = "DOCKERFILE"
dockerfilePath = "services/graph-proxy/Dockerfile"

[deploy]
startCommand = "python -m uvicorn services.graph-proxy.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300

[build]
builder = "DOCKERFILE"
dockerfilePath = "services/progress-tracker/Dockerfile"

[deploy]
startCommand = "python -m uvicorn services.progress-tracker.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300

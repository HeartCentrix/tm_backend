[build]
builder = "DOCKERFILE"
dockerfilePath = "services/backup-scheduler/Dockerfile"

[deploy]
startCommand = "python init_db.py && python -m uvicorn services.backup-scheduler.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
